export enum QuestionType {
  TEXT = 'text',
  NUMBER = 'number',
  EMAIL = 'email',
  COLOR = 'color',
  DATE = 'date',
  TIME = 'time',
  CHECKBOX = 'checkbox',
  RADIO = 'radio',
  SELECT = 'select',
}
